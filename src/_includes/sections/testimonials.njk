{% from "macros/testimonial.njk" import testimonialCard %}

<section class="section">
    <div class="container">
        <div class="section-heading text-center">
            <h2>Testimonials</h2>
        </div>
        <div class="row g-3">
            {% for testimonial in testimonials%}
                {{ testimonialCard(testimonial.name, testimonial.company, testimonial.rating, testimonial.testimonial) }}
            {% endfor %}
        </div>
    </div>
</section>